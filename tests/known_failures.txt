# Known-acceptable test failures — the machine-checked "pre-existing" ledger.
#
# A failure listed here is treated as pre-existing and does NOT fail the gate.
# Any failure NOT listed here is a REGRESSION: tools/check_test_baseline.py exits
# non-zero and names it. This file is the single source of truth — never call a
# failure "pre-existing" by eyeball; if it's real and unavoidable, it goes here
# with a reason, otherwise it gets fixed. When a listed test starts passing, the
# gate tells you to delete its line (keep the ledger honest and shrinking).
#
# Format: one "<suite>:<test-id>" per line. <suite> is "pytest" or "ctest".
#   pytest id = the node id, e.g. pytest:tests/unit/test_foo.py::test_bar
#   ctest id  = the test name,  e.g. ctest:FrameTest.Baz
# Blank lines and lines starting with '#' are ignored.

# --- C++ (ctest) ---------------------------------------------------------------
# (none — the C++ suite is expected fully green. The 7 scorch/heat-glow
# FrameTests formerly listed here were never a headless-GL artifact: commit
# 5739e1b5 un-negated the shader's decal normal gate to dot(n_body, dn) and the
# tests kept seeding inward -Z normals, zeroing every decal term. Fixed by
# seeding outward normals, matching ray_trace's shooter-facing convention.)

# --- Python (pytest) -----------------------------------------------------------
# Pre-existing, order-dependent PowerDisplay/announce test-harness fragility, NOT
# caused by SP2 (the CharacterClass animation-queue work) — proven, not eyeballed:
#   * The only SP2-changed code this test touches is IsAnimatingNonInterruptable();
#     a fixture-level probe shows the Engineer idle (busy=0, empty queue) at the
#     announce, so the announce never bails on anything SP2 changed.
#   * The actual failure is inside Bridge/PowerDisplay.py (an SDK HUD widget) that a
#     SetPlayer BROADCAST init's even in this UI-less test: Init divides main/(main+
#     backup) conduit capacity, which is 0 because the fixture never sets it and
#     GlobalPropertyTemplates doesn't provide it. PowerDisplay / subsystems / the
#     emitters test are all UNTOUCHED by SP2.
#   * Order-dependent: 5 of the emitters announce tests fail in isolation, but only
#     this one fails in the full-suite (gate) ordering; the 3 menu-driven variants
#     pass — a split orthogonal to animation.
# Proper fix (separate task): make the emitters fixture self-contained (set conduit
# capacity) AND stop PowerDisplay's headless Init/Update from crashing on widgets
# the test never builds. Tracked as a follow-up; delete this line once fixed.
pytest:tests/unit/test_engineer_emitters.py::test_shield_level_change_announces
